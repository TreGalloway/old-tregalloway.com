export declare type SetupParams = {
    connectionString: string;
    dirname: string;
};
export declare function setupCockroach(options: SetupParams): Promise<void>;
export declare function tearDownCockroach(options: SetupParams): Promise<void>;
