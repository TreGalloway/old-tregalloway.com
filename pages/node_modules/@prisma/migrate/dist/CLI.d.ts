import type { Command, Commands } from '@prisma/sdk';
import { HelpError } from '@prisma/sdk';
/**
 * CLI command
 */
export declare class CLI implements Command {
    private readonly cmds;
    static new(cmds: Commands): CLI;
    private constructor();
    parse(argv: string[]): Promise<string | Error>;
    help(error?: string): string | HelpError;
    private static help;
}
