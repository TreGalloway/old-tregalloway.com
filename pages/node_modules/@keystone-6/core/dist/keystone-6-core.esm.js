import { mergeSchemas } from '@graphql-tools/schema';
export { g as graphql } from './graphql-ts-schema-57db0426.esm.js';
import '@graphql-ts/schema';
import 'graphql-type-json';
import 'graphql-upload/GraphQLUpload.js';
import 'graphql';
import 'decimal.js';
import '@graphql-ts/schema/api-without-context';
import '@graphql-ts/extend';
import '@graphql-ts/schema/api-with-context';

function config(config) {
  return config;
}
function list(config) {
  return config;
}
function gql(strings) {
  return strings[0];
}
function graphQLSchemaExtension(_ref) {
  let {
    typeDefs,
    resolvers
  } = _ref;
  return schema => mergeSchemas({
    schemas: [schema],
    typeDefs,
    resolvers
  });
}

export { config, gql, graphQLSchemaExtension, list };
