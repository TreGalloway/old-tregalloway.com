import { jsx } from '@keystone-ui/core';
import { ListMeta } from '../../../../types';
export declare function SortSelection({ list, orderableFields, }: {
    list: ListMeta;
    orderableFields: Set<string>;
}): jsx.JSX.Element;
