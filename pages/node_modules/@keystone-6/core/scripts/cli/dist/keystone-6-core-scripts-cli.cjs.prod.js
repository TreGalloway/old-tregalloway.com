'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var meow = require('meow');
var _objectSpread = require('@babel/runtime/helpers/objectSpread2');
var _objectWithoutProperties = require('@babel/runtime/helpers/objectWithoutProperties');
var path = require('path');
var url = require('url');
var util = require('util');
var http = require('http');
var express = require('express');
var graphql = require('graphql');
var fs = require('fs-extra');
var chalk = require('chalk');
var initConfig = require('../../../dist/initConfig-b0833c64.cjs.prod.js');
var migrations = require('../../../dist/migrations-81592938.cjs.prod.js');
var requireSource = require('../../../dist/requireSource-07dc5fc7.cjs.prod.js');
var createAdminUIMiddleware = require('../../../dist/createAdminUIMiddleware-7f018fad.cjs.prod.js');
var artifacts_dist_keystone6CoreArtifacts = require('../../../dist/artifacts-979073dc.cjs.prod.js');
var utils = require('../../../dist/utils-f651a2f8.cjs.prod.js');
var session_dist_keystone6CoreSession = require('../../../session/dist/keystone-6-core-session.cjs.prod.js');
var typesForLists = require('../../../dist/types-for-lists-c2e856a0.cjs.prod.js');
var execa = require('execa');
require('p-limit');
require('../../../dist/core-ee045966.cjs.prod.js');
require('../../../dist/mysql-0e52fe45.cjs.prod.js');
require('../../../dist/graphql-ts-schema-2c0013b2.cjs.prod.js');
require('@graphql-ts/schema');
require('graphql-type-json');
require('graphql-upload/GraphQLUpload.js');
require('decimal.js');
require('@graphql-ts/schema/api-without-context');
require('@graphql-ts/extend');
require('@graphql-ts/schema/api-with-context');
require('@graphql-tools/schema');
require('../../../dist/graphql-errors-60425d62.cjs.prod.js');
require('apollo-server-errors');
require('resolve');
require('@nodelib/fs.walk');
require('@emotion/hash');
require('../../../dist/admin-meta-graphql-a3f15f1c.cjs.prod.js');
require('@apollo/client');
require('@babel/runtime/helpers/classPrivateFieldInitSpec');
require('@babel/runtime/helpers/classPrivateFieldGet');
require('@babel/runtime/helpers/classPrivateFieldSet');
require('@babel/runtime/helpers/defineProperty');
require('uuid');
require('image-type');
require('image-size');
require('stream');
require('@aws-sdk/s3-request-presigner');
require('@aws-sdk/client-s3');
require('@aws-sdk/lib-storage');
require('crypto');
require('filenamify');
require('@sindresorhus/slugify');
require('cuid');
require('../../../dist/package-path-778df3f1.cjs.prod.js');
require('@prisma/sdk');
require('@prisma/migrate');
require('pirates');
require('@babel/core');
require('source-map-support');
require('cors');
require('graphql-upload/graphqlUploadExpress.js');
require('../../../dist/createApolloServer-91d74e5f.cjs.prod.js');
require('apollo-server-micro');
require('apollo-server-express');
require('apollo-server-core');
require('module');
require('prettier');
require('prompts');
require('cookie');
require('@hapi/iron');
require('uid-safe');
require('graphql/execution/values');
require('pluralize');

function _interopDefault (e) { return e && e.__esModule ? e : { 'default': e }; }

function _interopNamespace(e) {
  if (e && e.__esModule) return e;
  var n = Object.create(null);
  if (e) {
    Object.keys(e).forEach(function (k) {
      if (k !== 'default') {
        var d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: function () { return e[k]; }
        });
      }
    });
  }
  n["default"] = e;
  return Object.freeze(n);
}

var meow__default = /*#__PURE__*/_interopDefault(meow);
var path__default = /*#__PURE__*/_interopDefault(path);
var url__default = /*#__PURE__*/_interopDefault(url);
var util__default = /*#__PURE__*/_interopDefault(util);
var express__default = /*#__PURE__*/_interopDefault(express);
var fs__namespace = /*#__PURE__*/_interopNamespace(fs);
var chalk__default = /*#__PURE__*/_interopDefault(chalk);
var execa__default = /*#__PURE__*/_interopDefault(execa);

async function buildAdminUI(projectAdminPath) {
  let prevNodeEnv =         "production"; // Next does a broken build unless we set NODE_ENV to production
  // @ts-ignore

  process.env.NODE_ENV = 'production';

  try {
    // importing next/dist/build is quite expensive so we're requiring it lazily

    /** We do this to stop webpack from bundling next inside of next */
    const next = 'next/dist/build';

    const build = require(next).default;

    await build(projectAdminPath);
  } finally {
    {
      // @ts-ignore
      process.env.NODE_ENV = prevNodeEnv;
    }
  }
}

const _excluded = ["server"],
      _excluded2 = ["extendHttpServer"],
      _excluded3 = ["adminMeta", "graphQLSchema", "createContext", "prismaSchema", "apolloServer"];
const devLoadingHTMLFilepath = path__default["default"].join(path__default["default"].dirname(require.resolve('@keystone-6/core/package.json')), 'static', 'dev-loading.html');

const wait = ms => new Promise(resolve => setTimeout(resolve, ms));

const cleanConfig = config => {
  const {
    server
  } = config,
        rest = _objectWithoutProperties(config, _excluded);

  if (server) {
    const restServer = _objectWithoutProperties(server, _excluded2);

    return _objectSpread(_objectSpread({}, rest), {}, {
      server: restServer
    });
  }

  return rest;
};

const dev = async (cwd, shouldDropDatabase) => {
  var _config$server;

  console.log('✨ Starting Keystone');
  const app = express__default["default"]();
  let expressServer = null;
  const httpServer = http.createServer(app);
  let hasAddedAdminUIMiddleware = false;
  let disconnect = null; // note that because we don't catch this throwing, if this fails it'll crash the process
  // so that means if
  // - you have an error in your config on startup -> will fail to start and you have to start the process manually after fixing the problem
  // - you have an error in your config after startup -> will keep the last working version until importing the config succeeds
  // also, if you're thinking "why not always use the Next api route to get the config"?
  // this will get the GraphQL API up earlier

  const configWithHTTP = initConfig.initConfig(requireSource.requireSource(utils.getConfigPath(cwd)).default);
  const config = cleanConfig(configWithHTTP);

  const isReady = () => {
    var _config$ui;

    return expressServer !== null && (hasAddedAdminUIMiddleware || ((_config$ui = config.ui) === null || _config$ui === void 0 ? void 0 : _config$ui.isDisabled) === true);
  };

  const initKeystone = async () => {
    var _configWithHTTP$serve, _config$ui2;

    await fs__namespace["default"].remove(utils.getAdminPath(cwd));
    const p = initConfig.serializePathForImport(path__default["default"].relative(path__default["default"].join(utils.getAdminPath(cwd), 'pages', 'api'), `${cwd}/keystone`));

    const _await$setupInitialKe = await setupInitialKeystone(config, cwd, shouldDropDatabase),
          {
      adminMeta,
      graphQLSchema,
      createContext,
      prismaSchema,
      apolloServer
    } = _await$setupInitialKe,
          rest = _objectWithoutProperties(_await$setupInitialKe, _excluded3);

    if (configWithHTTP !== null && configWithHTTP !== void 0 && (_configWithHTTP$serve = configWithHTTP.server) !== null && _configWithHTTP$serve !== void 0 && _configWithHTTP$serve.extendHttpServer) {
      const createRequestContext = async (req, res) => createContext({
        sessionContext: config.session ? await session_dist_keystone6CoreSession.createSessionContext(config.session, req, res, createContext) : undefined,
        req
      });

      configWithHTTP.server.extendHttpServer(httpServer, createRequestContext, graphQLSchema);
    }

    const prismaClient = createContext().prisma;
    ({
      disconnect,
      expressServer
    } = rest); // if you've disabled the Admin UI, sorry, no live reloading
    // the chance that someone is actually using this is probably quite low
    // and starting Next in tests where we don't care about it would slow things down quite a bit

    if ((_config$ui2 = config.ui) !== null && _config$ui2 !== void 0 && _config$ui2.isDisabled) {
      initKeystonePromiseResolve();
      return;
    }

    const adminUIMiddleware = await initAdminUI(config, graphQLSchema, adminMeta, cwd, createContext);
    expressServer.use(adminUIMiddleware);
    hasAddedAdminUIMiddleware = true;
    initKeystonePromiseResolve(); // this exports a function which dynamically requires the config rather than directly importing it.
    // this allows us to control exactly _when_ the gets evaluated so that we can handle errors ourselves.
    // note this is intentionally using CommonJS and not ESM because by doing a dynamic import, webpack
    // will generate a separate chunk for that whereas a require will be in the same bundle but the execution can still be delayed.
    // dynamic importing didn't work on windows because it couldn't get the path to require the chunk for some reason

    await fs__namespace["default"].outputFile(`${utils.getAdminPath(cwd)}/pages/api/__keystone_api_build.js`, `exports.getConfig = () => require(${p});
const x = Math.random();
exports.default = function (req, res) { return res.send(x.toString()) }
`);
    let lastVersion = '';
    let lastError = undefined;
    const originalPrismaSchema = artifacts_dist_keystone6CoreArtifacts.printPrismaSchema(typesForLists.initialiseLists(config), config.db.provider, config.db.prismaPreviewFeatures, config.db.additionalPrismaDatasourceProperties);
    let lastPrintedGraphQLSchema = graphql.printSchema(graphQLSchema);
    let lastApolloServer = apolloServer;

    while (true) {
      await wait(500);

      try {
        // this fetching essentially does two things:
        // - keeps the api route built, if we don't fetch it, Next will stop compiling it
        // - returns a random number which when it changes indicates that the config _might_ have changed
        //   note that it can go off randomly so the version changing doesn't necessarily
        //   mean that the config has changed, Next might have just reloaded for some random reason
        //   so we shouldn't log something like "hey, we reloaded your config"
        //   because it would go off at times when the user didn't change their config
        const version = await fetch(`http://localhost:${httpOptions.port}/api/__keystone_api_build`).then(x => x.text());

        if (lastVersion !== version) {
          lastVersion = version;

          const resolved = require.resolve(`${utils.getAdminPath(cwd)}/.next/server/pages/api/__keystone_api_build`);

          delete require.cache[resolved]; // webpack will make modules that import Node ESM externals(which must be loaded with dynamic import)
          // export a promise that resolves to the actual export so yeah, we need to await a require call
          // technically, the await for requiring the api route module isn't necessary since there are no imports there
          // but just in case webpack decides to make it async in the future, this'll still work

          const apiRouteModule = await require(resolved);
          const uninitializedConfig = (await apiRouteModule.getConfig()).default;
          const newConfigWithHttp = initConfig.initConfig(uninitializedConfig);
          const newConfig = cleanConfig(newConfigWithHttp);
          const newPrismaSchema = artifacts_dist_keystone6CoreArtifacts.printPrismaSchema(typesForLists.initialiseLists(newConfig), newConfig.db.provider, newConfig.db.prismaPreviewFeatures, newConfig.db.additionalPrismaDatasourceProperties);

          if (originalPrismaSchema !== newPrismaSchema) {
            console.log('🔄 Your prisma schema has changed, please restart Keystone');
            process.exit(1);
          } // we only need to test for the things which influence the prisma client creation
          // and aren't written into the prisma schema since we check whether the prisma schema has changed above


          if (newConfig.db.enableLogging !== config.db.enableLogging || newConfig.db.url !== config.db.url || newConfig.db.useMigrations !== config.db.useMigrations) {
            console.log('Your db config has changed, please restart Keystone');
            process.exit(1);
          }

          const {
            graphQLSchema,
            getKeystone,
            adminMeta
          } = initConfig.createSystem(newConfig, true); // we're not using generateCommittedArtifacts or any of the similar functions
          // because we will never need to write a new prisma schema here
          // and formatting the prisma schema leaves some listeners on the process
          // which means you get a "there's probably a memory leak" warning from node

          const newPrintedGraphQLSchema = graphql.printSchema(graphQLSchema);

          if (newPrintedGraphQLSchema !== lastPrintedGraphQLSchema) {
            await fs__namespace["default"].writeFile(artifacts_dist_keystone6CoreArtifacts.getSchemaPaths(cwd).graphql, artifacts_dist_keystone6CoreArtifacts.getFormattedGraphQLSchema(newPrintedGraphQLSchema));
            lastPrintedGraphQLSchema = newPrintedGraphQLSchema;
          }

          await artifacts_dist_keystone6CoreArtifacts.generateNodeModulesArtifactsWithoutPrismaClient(graphQLSchema, newConfig, cwd);
          await initConfig.generateAdminUI(newConfig, graphQLSchema, adminMeta, utils.getAdminPath(cwd), true);
          const keystone = getKeystone(function fakePrismaClientClass() {
            return prismaClient;
          });
          await keystone.connect();
          const servers = await createAdminUIMiddleware.createExpressServer(newConfig, graphQLSchema, keystone.createContext);
          servers.expressServer.use(adminUIMiddleware);
          expressServer = servers.expressServer;
          let prevApolloServer = lastApolloServer;
          lastApolloServer = servers.apolloServer;
          await prevApolloServer.stop();
          lastError = undefined;
        }
      } catch (err) {
        // since Next will sometimes randomly refresh the api route even though it hasn't changed
        // we want to avoid showing the same error again
        const printed = util__default["default"].inspect(err);

        if (printed !== lastError) {
          console.log('🚨', chalk__default["default"].red('There was an error loading your Keystone config'));
          console.log(printed);
          lastError = printed;
        }
      }
    }
  }; // You shouldn't really be doing a healthcheck on the dev server, but we
  // respond on the endpoint with the correct error code just in case. This
  // doesn't send the configured data shape, because config doesn't allow
  // for the "not ready" case but that's probably OK.


  if ((_config$server = config.server) !== null && _config$server !== void 0 && _config$server.healthCheck) {
    const healthCheckPath = config.server.healthCheck === true ? createAdminUIMiddleware.defaults.healthCheckPath : config.server.healthCheck.path || createAdminUIMiddleware.defaults.healthCheckPath;
    app.use(healthCheckPath, (req, res, next) => {
      if (expressServer) return next();
      res.status(503).json({
        status: 'fail',
        timestamp: Date.now()
      });
    });
  } // Serve the dev status page for the Admin UI


  app.use('/__keystone_dev_status', (req, res) => {
    res.json({
      ready: isReady() ? true : false
    });
  }); // Pass the request the express server, or serve the loading page

  app.use((req, res, next) => {
    var _config$ui3, _config$graphql;

    // If both the express server and Admin UI Middleware are ready, we're go!
    if (expressServer && (hasAddedAdminUIMiddleware || ((_config$ui3 = config.ui) === null || _config$ui3 === void 0 ? void 0 : _config$ui3.isDisabled) === true)) {
      return expressServer(req, res, next);
    } // Otherwise, we may be able to serve the GraphQL API


    const {
      pathname
    } = url__default["default"].parse(req.url);

    if (expressServer && pathname === (((_config$graphql = config.graphql) === null || _config$graphql === void 0 ? void 0 : _config$graphql.path) || '/api/graphql')) {
      return expressServer(req, res, next);
    } // Serve the loading page


    res.sendFile(devLoadingHTMLFilepath);
  });
  let initKeystonePromiseResolve;
  let initKeystonePromiseReject;
  let initKeystonePromise = new Promise((resolve, reject) => {
    initKeystonePromiseResolve = resolve;
    initKeystonePromiseReject = reject;
  });
  const httpOptions = {
    port: 3000
  };

  if (config !== null && config !== void 0 && config.server && 'port' in config.server) {
    httpOptions.port = config.server.port;
  }

  if (config !== null && config !== void 0 && config.server && 'options' in config.server && config.server.options) {
    Object.assign(httpOptions, config.server.options);
  } // preference env.PORT if supplied


  if ('PORT' in process.env) {
    httpOptions.port = parseInt(process.env.PORT || '');
  }

  const server = httpServer.listen(httpOptions, err => {
    var _config$graphql2;

    if (err) throw err; // We start initialising Keystone after the dev server is ready,

    console.log(`⭐️ Dev Server Starting on http://localhost:${httpOptions.port}`);
    console.log(`⭐️ GraphQL API Starting on http://localhost:${httpOptions.port}${((_config$graphql2 = config.graphql) === null || _config$graphql2 === void 0 ? void 0 : _config$graphql2.path) || '/api/graphql'}`); // Don't start initialising Keystone until the dev server is ready,
    // otherwise it slows down the first response significantly

    initKeystone().catch(err => {
      server.close(async closeErr => {
        if (closeErr) {
          console.log('There was an error while closing the server');
          console.log(closeErr);
        }

        try {
          var _disconnect;

          await ((_disconnect = disconnect) === null || _disconnect === void 0 ? void 0 : _disconnect());
        } catch (err) {
          console.log('There was an error while disconnecting from the database');
          console.log(err);
        }

        initKeystonePromiseReject(err);
      });
    });
  });
  await initKeystonePromise;
  return () => new Promise((resolve, reject) => {
    server.close(async err => {
      try {
        var _disconnect2;

        await ((_disconnect2 = disconnect) === null || _disconnect2 === void 0 ? void 0 : _disconnect2());
      } catch (disconnectionError) {
        if (!err) {
          err = disconnectionError;
        } else {
          console.log('There was an error while disconnecting from the database');
          console.log(disconnectionError);
        }
      }

      if (err) {
        reject(err);
      } else {
        resolve();
      }
    });
  });
};

async function setupInitialKeystone(config, cwd, shouldDropDatabase) {
  const {
    graphQLSchema,
    adminMeta,
    getKeystone
  } = initConfig.createSystem(config); // Generate the Artifacts

  console.log('✨ Generating GraphQL and Prisma schemas');
  const prismaSchema = (await artifacts_dist_keystone6CoreArtifacts.generateCommittedArtifacts(graphQLSchema, config, cwd)).prisma;
  let keystonePromise = artifacts_dist_keystone6CoreArtifacts.generateNodeModulesArtifacts(graphQLSchema, config, cwd).then(() => {
    const prismaClient = artifacts_dist_keystone6CoreArtifacts.requirePrismaClient(cwd);
    return getKeystone(prismaClient);
  });
  let migrationPromise; // Set up the Database

  if (config.db.useMigrations) {
    migrationPromise = migrations.devMigrations(config.db.url, config.db.shadowDatabaseUrl, prismaSchema, artifacts_dist_keystone6CoreArtifacts.getSchemaPaths(cwd).prisma, shouldDropDatabase);
  } else {
    migrationPromise = migrations.pushPrismaSchemaToDatabase(config.db.url, config.db.shadowDatabaseUrl, prismaSchema, artifacts_dist_keystone6CoreArtifacts.getSchemaPaths(cwd).prisma, shouldDropDatabase);
  }

  const [keystone] = await Promise.all([keystonePromise, migrationPromise]);
  const {
    createContext
  } = keystone; // Connect to the Database

  console.log('✨ Connecting to the database');
  await keystone.connect(); // Set up the Express Server

  console.log('✨ Creating server');
  const {
    apolloServer,
    expressServer
  } = await createAdminUIMiddleware.createExpressServer(config, graphQLSchema, createContext);
  console.log(`✅ GraphQL API ready`); // Make local storage folders if used

  for (const val of Object.values(config.storage || {})) {
    if (val.kind !== 'local') continue;
    fs__namespace["default"].mkdirSync(val.storagePath, {
      recursive: true
    });
    console.warn(`WARNING: 'mkdir -p ${val.storagePath}' won't happen in production`);
  }

  return {
    adminMeta,
    disconnect: () => keystone.disconnect(),
    expressServer,
    apolloServer,
    graphQLSchema,
    createContext,
    prismaSchema
  };
}

async function initAdminUI(config, graphQLSchema, adminMeta, cwd, createContext) {
  console.log('✨ Generating Admin UI code');
  await initConfig.generateAdminUI(config, graphQLSchema, adminMeta, utils.getAdminPath(cwd), false);
  console.log('✨ Preparing Admin UI app');
  const middleware = await createAdminUIMiddleware.createAdminUIMiddleware(config, createContext, true, utils.getAdminPath(cwd));
  console.log(`✅ Admin UI ready`);
  return middleware;
}

const start = async cwd => {
  var _config$ui;

  console.log('✨ Starting Keystone'); // This is the compiled version of the configuration which was generated during the build step.
  // See reexportKeystoneConfig().

  const apiFile = path__default["default"].join(utils.getAdminPath(cwd), '.next/server/pages/api/__keystone_api_build.js');

  if (!fs__namespace.existsSync(apiFile)) {
    console.log('🚨 keystone build must be run before running keystone start');
    throw new utils.ExitError(1);
  } // webpack will make modules that import Node ESM externals(which must be loaded with dynamic import)
  // export a promise that resolves to the actual export so yeah, we need to await a require call


  const config = initConfig.initConfig((await require(apiFile)).config);
  const {
    getKeystone,
    graphQLSchema
  } = initConfig.createSystem(config);
  const prismaClient = artifacts_dist_keystone6CoreArtifacts.requirePrismaClient(cwd);
  const keystone = getKeystone(prismaClient);
  console.log('✨ Connecting to the database');
  await keystone.connect();
  console.log('✨ Creating server');
  const {
    expressServer,
    httpServer
  } = await createAdminUIMiddleware.createExpressServer(config, graphQLSchema, keystone.createContext);
  console.log(`✅ GraphQL API ready`);

  if (!((_config$ui = config.ui) !== null && _config$ui !== void 0 && _config$ui.isDisabled)) {
    console.log('✨ Preparing Admin UI Next.js app');
    expressServer.use(await createAdminUIMiddleware.createAdminUIMiddleware(config, keystone.createContext, false, utils.getAdminPath(cwd)));
    console.log(`✅ Admin UI ready`);
  }

  const options = {
    port: 3000
  };

  if (config !== null && config !== void 0 && config.server && 'port' in config.server) {
    options.port = config.server.port;
  }

  if (config !== null && config !== void 0 && config.server && 'options' in config.server && config.server.options) {
    Object.assign(options, config.server.options);
  } // preference env.PORT if supplied


  if ('PORT' in process.env) {
    options.port = parseInt(process.env.PORT || '');
  }

  httpServer.listen(options, err => {
    if (err) throw err;
    console.log(`⭐️ Server Ready on http://localhost:${options.port}`);
  });
};

const reexportKeystoneConfig = async (cwd, isDisabled) => {
  const projectAdminPath = utils.getAdminPath(cwd);
  const configPath = utils.getConfigPath(cwd);

  if (isDisabled) {
    // Nuke any existing files in our target directory
    await fs__namespace["default"].remove(projectAdminPath);
  } // We re-export the Keystone config file into the Admin UI project folder
  // so that when we run the build step, we will end up with a compiled version
  // of the configuration file in the .next/ directory. Even if we're not building
  // an Admin UI, we still need to run the `build()` function so that this config
  // file is correctly compiled.


  const pkgDir = path__default["default"].dirname(require.resolve('@keystone-6/core/package.json'));
  const p = initConfig.serializePathForImport(path__default["default"].relative(path__default["default"].join(projectAdminPath, 'pages', 'api'), configPath));
  const files = [{
    mode: 'write',
    src: `export { default as config } from ${p};
            export default function (req, res) { return res.status(500) }`,
    outputPath: path__default["default"].join('pages', 'api', '__keystone_api_build.js')
  }];

  if (isDisabled) {
    // These are the basic files required to have a valid Next.js project. If the
    // Admin UI is disabled then we need to do this ourselves here.
    files.push({
      mode: 'copy',
      inputPath: path__default["default"].join(pkgDir, 'static', 'next.config.js'),
      outputPath: 'next.config.js'
    }, {
      mode: 'copy',
      inputPath: path__default["default"].join(pkgDir, 'static', 'tsconfig.json'),
      outputPath: 'tsconfig.json'
    });
  }

  await Promise.all(files.map(file => initConfig.writeAdminFile(file, projectAdminPath)));
};

async function build(cwd) {
  var _config$ui, _config$ui2;

  const config = initConfig.initConfig(requireSource.requireSource(utils.getConfigPath(cwd)).default);
  const {
    graphQLSchema,
    adminMeta
  } = initConfig.createSystem(config);
  await artifacts_dist_keystone6CoreArtifacts.validateCommittedArtifacts(graphQLSchema, config, cwd);
  console.log('✨ Building Keystone'); // FIXME: This needs to generate clients for the correct build target using binaryTarget
  // https://www.prisma.io/docs/reference/api-reference/prisma-schema-reference#binarytargets-options

  await artifacts_dist_keystone6CoreArtifacts.generateNodeModulesArtifacts(graphQLSchema, config, cwd);

  if ((_config$ui = config.ui) !== null && _config$ui !== void 0 && _config$ui.isDisabled) {
    console.log('✨ Skipping Admin UI code generation');
  } else {
    console.log('✨ Generating Admin UI code');
    await initConfig.generateAdminUI(config, graphQLSchema, adminMeta, utils.getAdminPath(cwd), false);
  }

  console.log('✨ Generating Keystone config code');
  await reexportKeystoneConfig(cwd, (_config$ui2 = config.ui) === null || _config$ui2 === void 0 ? void 0 : _config$ui2.isDisabled);
  console.log('✨ Building Admin UI');
  await buildAdminUI(utils.getAdminPath(cwd));
}

async function prisma(cwd, args) {
  const config = initConfig.initConfig(requireSource.requireSource(utils.getConfigPath(cwd)).default);
  const {
    graphQLSchema
  } = initConfig.createSystem(config);
  await artifacts_dist_keystone6CoreArtifacts.validateCommittedArtifacts(graphQLSchema, config, cwd);
  await artifacts_dist_keystone6CoreArtifacts.generateNodeModulesArtifacts(graphQLSchema, config, cwd);
  const result = await execa__default["default"]('node', [require.resolve('prisma'), ...args], {
    cwd,
    stdio: 'inherit',
    reject: false,
    env: _objectSpread(_objectSpread({}, process.env), {}, {
      DATABASE_URL: config.db.url,
      PRISMA_HIDE_UPDATE_MESSAGE: '1'
    })
  });

  if (result.exitCode !== 0) {
    throw new utils.ExitError(result.exitCode);
  }
}

// There’s some files that we need to generate into node_modules and it’s
// important to have them available immediately so things like TypeScript
// won’t fail.
// We want to validate that your Prisma and GraphQL schemas are up to date
// to prevent the awkward “this is changing because of a previous PR that
// didn’t update these things”.
// Why do validation in the postinstall rather than a separate validate command?
//
// It means that it’s hard to get it wrong. You have to run the postinstall
// script anyway so it prevents “oh no, you forgot add this to your CI”
// node_modules
//   .prisma/client (this is where Prisma generates the client to by default,
//      this means we can have a conversation about whether we should tell
//      people to use @prisma/client directly for certain things though we
//      are not necessarily saying that’s what we should do)
//   .keystone
//     - All .js files will have a corresponding .d.ts file for TypeScript.
//       We are generating vanilla JavaScript because:
//         * the user may not be using TypeScript
//         * we can’t/shouldn’t rely on files in node_modules being transpiled even if they are
//     - types.{js,.ts}: .d.ts will be the same as current .keystone/schema-types.ts, the .js will be empty
//     - next/graphql-api.js: to be imported into a next app as an api route
//         * only generated with generateNextGraphqlAPI option
//     - api.js:  includes the lists API (full exports to be decided elsewhere
//         * only generated with generateNodeAPI option

async function postinstall(cwd, shouldFix) {
  const config = initConfig.initConfig(requireSource.requireSource(utils.getConfigPath(cwd)).default);
  const {
    graphQLSchema
  } = initConfig.createSystem(config);

  if (shouldFix) {
    await artifacts_dist_keystone6CoreArtifacts.generateCommittedArtifacts(graphQLSchema, config, cwd);
    console.log('✨ Generated GraphQL and Prisma schemas');
  } else {
    await artifacts_dist_keystone6CoreArtifacts.validateCommittedArtifacts(graphQLSchema, config, cwd);
    console.log('✨ GraphQL and Prisma schemas are up to date');
  }

  await artifacts_dist_keystone6CoreArtifacts.generateNodeModulesArtifacts(graphQLSchema, config, cwd);
}

const commands = {
  dev,
  start,
  build,
  prisma,
  postinstall
};
async function cli(cwd, argv) {
  const {
    input,
    help,
    flags
  } = meow__default["default"](`
    Usage
      $ keystone [command]
    Commands
        dev           start the project in development mode (default)
        postinstall   generate client APIs and types (optional)
        build         build the project (must be done before using start)
        start         start the project in production mode
        prisma        run Prisma CLI commands safely
    `, {
    flags: {
      fix: {
        default: false,
        type: 'boolean'
      },
      resetDb: {
        default: false,
        type: 'boolean'
      }
    },
    argv
  });
  const command = input[0] || 'dev';

  if (!isCommand(command)) {
    console.log(`${command} is not a command that keystone accepts`);
    console.log(help);
    throw new utils.ExitError(1);
  }

  if (command === 'prisma') {
    return prisma(cwd, argv.slice(1));
  } else if (command === 'postinstall') {
    return postinstall(cwd, flags.fix);
  } else if (command === 'dev') {
    return dev(cwd, flags.resetDb);
  } else {
    return commands[command](cwd);
  }
}

function isCommand(command) {
  return command in commands;
}

exports.cli = cli;
